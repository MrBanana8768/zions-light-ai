# Most of these are vllm transitive deps; listed here to make the
# compactor's direct dependencies explicit. Be VERY careful with pins:
#
#   v1.9     pinned everything (transformers==4.47.1) — too old, missing
#            Gemma3Config that modern vLLM imports unconditionally.
#   v1.9.1/2 unpinned everything — pip resolved transformers to 5.9.0
#            which removed `all_special_tokens_extended` that vLLM 0.11
#            still calls. Tokenizer init crashes with AttributeError.
#   v1.9.4   keep fastapi/uvicorn/httpx unpinned (we don't care which
#            version, as long as vllm and compactor agree), but pin
#            transformers to the 4.5x line that vLLM 0.11 was tested
#            against. Bump the upper bound when bumping VLLM_VERSION.
fastapi
uvicorn[standard]
httpx
transformers>=4.50,<5
