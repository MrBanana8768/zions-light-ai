# Fixture server deps. Pinned to the same majors the compactor uses
# (compactor/requirements.txt) so the two sides of the contract are exercised
# by the same tokenizer library the production compactor links against.
fastapi==0.138.2
uvicorn[standard]==0.49.0
transformers==5.12.1
jinja2==3.1.6
